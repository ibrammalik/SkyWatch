import { Container, Typography, Button } from "@mui/material";

const Home = () => (
  <Container>
    <Typography variant="h4" gutterBottom>
      Ini Halaman About
    </Typography>
    <Button variant="contained" color="primary">
      Halo, Saya Maulana Malik Ibrahim!!
    </Button>
  </Container>
);

export default Home;
